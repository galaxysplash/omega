#pragma once

class special_symbols {
public:
  constexpr static inline char included_cutters[] = {
      ':', '=', ')', '(', '-', '*', '+', '/',
      '%', '$', ',', ';', '{', '}'
  };

  constexpr static inline char excluded_cutters[] = {
      
  };
};
